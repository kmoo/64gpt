# M13 — LLM-assisted phrase-bank distillation (planning — not started)

**Status: PLANNED.** Claimed the M13 slot 2026-07-26 from the portability
release, which moves to `docs/milestones/m14.md` (see that file's
Renumbering note #3) — this milestone is the active next-in-sequence
work; portability remains dormant ("PLANNED, not started" since the
original M11→M12 split) with nothing currently blocking on it.

No hard code dependency on the M12.2-M12.5 voice-conditioning line (this
is a different kind of lever, see "Why" below), but sequencing-wise it's
cleaner to start once `m12.3-voices`'s fate (merge or park) is decided,
so this doesn't build on top of an unresolved experimental branch.

## This is NOT M9's `llm_judge.py` — a different, bigger mechanism

`trainer/llm_judge.py` (built and run once, M9, commit `24458f3`, never
touched since) is a **pure rater**: it scores already-generated corpus
lines 1-5 on coherence/voice and writes a report. It never rewrites
anything and never feeds anything back into a training corpus — closer
to a linter than a training mechanism.

What this milestone actually is — LLM-authored content entering the
training corpus, not just grading it — is a materially bigger step, and
one this project has a directly relevant, cautionary data point for
already (next section). The plumbing (batched dispatch, JSON prompt/
parse, `opencoder` routing) should still be **reused and extended**, not
duplicated — see "Build on, don't rebuild" below.

## M12.6's negative result sharpens mechanism 2

M12.6 (`docs/milestones/m12.6.md`) hand-expanded korrath/elowen's bank
density to match shadewrath's, using only existing vocabulary/motifs,
integrated through the same template grammar (not one-off lines). It
did not close the gap — elowen actually regressed (3.00 vs M12.4's
1.88) — independently supporting mechanism 2's premise below: raw
bank-entry count is not the lever, even when well-integrated. This
milestone's bet is specifically on fragment *quality/structural fit*
(judge-vetted, density-gated), not just *quantity*.

## Why — and a retargeting from the original pitch

The original conversation proposal framed this as fixing "free-running
generation quality... coherence... avoidance of gibberish." That framing
needs a correction: **M12.1 already gave the shipped model a hard
structural guarantee against invented words** (the lexicon-trie decode
guard — 0 invented words across all 36 shipped goldens, not a
probability, a structural impossibility). Generic gibberish-avoidance is
not this project's open problem anymore.

What IS still open, per M12.4's own findings: getting the model to
reliably express D:/M: attribute conditioning on **thin-corpus
characters** — guard (1.75 inv/line) and korrath (2.00) lag well behind
selena (0.62) and cast (1.12) on the raw (unguarded) coherence probe,
even with M12.4's best conditioning-mechanism result so far (1.44
overall, gate ≤1.00). This milestone's real value is a **corpus-quality**
lever aimed specifically at that gap — complementary to the
conditioning-*mechanism* work (M12.2-M12.5), not a replacement for it.

## The cautionary precedent this design must not repeat

M9's Attempt #1 (`docs/milestones/m9.md` §4, `generate_m9_corpus_llm.py`)
already tried "use an LLM to author corpus text" once. `llm_judge.py`
rated that corpus **4.75/5 coherence, 4.18/5 voice** — looked great in
isolation. The **model actually trained on it** produced garbled output
scoring **3.36/5 coherence, 2.64/5 voice**, with real invented words
("FORENTURESS", "CAVILY", "COMEEFILUTHING"). Root cause, confirmed
against the corpora that do train cleanly: freeform LLM text has high
lexical diversity and almost no repetition (~1,300 chars/persona);
Selena and guard train cleanly because they're deterministic
template-grammar generation with high controlled repetition (~120K+
chars/character). **This is the single most important fact this
milestone must design around** — a judge rating text as "good" tells you
nothing about whether a ~100-400K-param char-GRU can learn from it.

M9's own Data Science Review flagged "does the LLM-judge's score
correlate with human judgment / real model quality" as explicitly
unresolved. As far as this review can find, **no later milestone ever
answered that question.** It's not just historically interesting — it's
the exact premise this milestone's whole approach rests on, still
unvalidated after 9 milestones. See mechanism 4 below.

## Four mechanisms, each closing off one way this repeats M9's mistake

### 1. Intervene at the bank level, not the line level

Don't judge/rewrite whole assembled lines and insert them as one-off
replacements — that's Attempt #1 with extra steps. Instead, have the LLM
propose new **bank fragments** for guard/korrath's existing
descriptor/context/tier banks (`guard_corpus.py`, `korrath_corpus.py`):
openers, body-skeleton templates, catchphrase entries — the same
ingredient categories M9's Attempt #2 already proved work
(`docs/milestones/m9.md` §4). Each accepted fragment gets woven through
the *existing* template-grammar generator and reused across dozens of
combos automatically, inheriting the repetition structure that already
trains cleanly, instead of being judged as an isolated sentence.

### 2. Make the density lesson an enforced, quantitative gate

M9's finding ("~120K+ chars/character, not ~1,300") becomes a literal
check, not a prompt instruction to hope the LLM respects: any new
bank fragment only survives if, once folded into the corpus generator,
it gets reused **≥K times** across distinct generated combos (K TBD
empirically, calibrated against guard/cast's own proven density). A
fragment used once is exactly Attempt #1's failure mode in miniature.

### 3. Split what the LLM is good at from what it isn't

A big LLM has no notion of what makes a tiny char-GRU trainable — that's
precisely why 4.75/5-rated text produced a 3.36/5 model. Run a cheap,
**programmatic structural-compatibility gate before any LLM call**:
reuse `ngpt_trainer.divergence.jaccard_distance` (already used for
near-duplicate detection in `llm_judge.py`) and `build_corpus_vocab`/
`invented_word_count` (`make_m12_1_blob.py`) to reject fragments with the
wrong repetition/density/vocabulary signature for free, before spending
any tokens. Only pay for LLM judgment on tone/voice plausibility — the
part it's actually good at — for candidates that already clear the
structural bar.

### 4. Actually close M9's open validation question, using M11.1's playbook

Before trusting judge-driven corpus changes for real: **pre-register a
bar before either result is seen** (M11.1's own discipline,
`docs/milestones/m11.1.md` Part 2 — the project's most rigorous
precedent for exactly this kind of "does the metric predict the real
outcome" question). Generate a handful of judge-approved and
judge-rejected bank-fragment sets, actually train quick models on each
(cheap at this project's scale — tiny-corpus overfit training already
runs in seconds), and check whether judge score predicts the real
downstream coherence-probe number. If it doesn't correlate, that's the
answer, and it's learned *before* building the rest of the pipeline on
top of an unvalidated premise — not after, the way M9 discovered its gap
by accident partway through.

### Mechanism 4's protocol, locked 2026-07-27 before any result was seen

Drafted with Luke, revised once after a data-science-style critique of
the first draft caught three real design flaws before they could bias
the result: (1) an uneven approved/rejected split size would confound
"judge quality" with "M12.6-proven quantity/density effects"; (2) a
single run per arm has no noise floor to compare against; (3) the
original pass bar (fixed percentage margins) was arbitrary, not
calibrated to anything. Fixed below.

**Scope:** guard and korrath only -- M12.4's two laggards (1.75 / 2.00
inv/line vs the <=1.00 gate).

**Steps:**
1. Generate candidate OPENER-style fragments for guard/korrath via one
   straightforward `opencoder` dispatch -- no deliberately-injected
   good/bad mix, to avoid biasing the split toward an easy result.
2. Run every candidate through the structural gate (mechanism 3,
   `corpus_gates.py`, already shipped).
3. Judge survivors 1-5 on tone/voice-plausibility, extending
   `llm_judge.py`'s dispatch/parse machinery with a new rubric (the
   D:/M: attribute values in play have to be part of the prompt, per
   mechanism 3's own note -- "fidelity to tone" is meaningless without
   knowing which voice archetype is judged).
4. Rank survivors by score. Take the **top-K as approved, bottom-K as
   rejected -- equal K** on both sides, fixing the quantity confound.
5. **Human spot-check (Luke) of the approved/rejected split, required
   before any training compute is spent** -- same discipline
   `llm_judge.py` already applies to whole lines (M9's Data Science
   Review), never previously extended to fragment-level judging.
6. Four full-cast retrains (guard/korrath share one model with the rest
   of the cast per M11.1's compositional design -- there's no isolated
   "just guard/korrath" run), same probe methodology as
   `trainer/m12_4_attr_ablation_spike.py`:
   - baseline, seed A
   - baseline, seed B (same corpus, different seed -- this run's ONLY
     purpose is measuring the noise floor)
   - baseline + approved-K fragments
   - baseline + rejected-K fragments
7. Measure invented/line for guard+korrath (and each individually) on
   all four runs.

**Pre-registered bar (self-calibrating, not a fixed percentage):**

```
noise_floor = |baseline_seedA - baseline_seedB|   (invented/line, guard+korrath)

PASS  if  approved <= baseline + noise_floor
      AND (rejected - approved) > noise_floor

INCONCLUSIVE  if the approved/rejected gap is within noise_floor of the
              cutoff -- reported as its own honest outcome, not forced
              into PASS or FAIL

FAIL  otherwise
```

PASS means the judge's tone/voice score predicts real training impact
well enough to trust for mechanism 1's real pipeline. FAIL or
INCONCLUSIVE means it doesn't (at this K, this rubric, this corpus) --
a valid stopping point for the LLM-authoring half of this milestone,
same discipline as every other honest negative this project has
recorded. Note what this bar does NOT claim: even a clean PASS only
means the judge is directionally trustworthy, not that this path can
reach the real <=1.00 target -- that's a separate, later question.

### Mechanism 4's execution record (2026-07-27/28) -- steps 1-5 run, step 6 deliberately paused

Steps 1-5 of the protocol above ran overnight. Step 6 (the four real
training retrains) was **deliberately not started** -- explained at the
end of this section, not a thing that was simply forgotten.

**Step 1, attempt 1 (blind generation, Mistral-7B, no vocab
constraint).** 24 candidates (guard/korrath, worried + one cheerful
slot). Sounded in-genre; 6/24 survived the structural gate at
`max_invented=0` -- every rejection was an ordinary English word
(`SIGNIFICANT`, `FOREBODING`, `BITTERSWEET`...) absent from this
project's deliberately narrow ~2,100-word corpus vocabulary, not a
quality problem.

**Step 1, attempt 2 (vocab-constrained generation) -- made it WORSE, not
better.** Handed Mistral the full corpus vocabulary as an explicit
"use only these words" constraint. Survival dropped to 1/32. Plausible
cause: surfacing those specific words as salient context may have
primed the model to reach for them even more. Real finding: a 7B model
via plain instruction-following cannot reliably respect a ~2,100-item
allow-list, constraint stated explicitly or not.

**Step 2 (structural gate).** Worked exactly as designed both times --
correctly rejected everything with real-word-but-out-of-corpus-vocab
content. Not itself in question at any point tonight.

**Step 3, first pass -- judge collapses to uniform per-group scores.**
A single 24-item batched dispatch (Mistral) returned two constant
values: every WORRIED item scored 5, every CHEERFUL item scored 4 --
zero within-group discrimination. An isolated 6-item batch (real
candidates only, no decoys) reproduced the same uniform-5 collapse.

**Calibration check -- decisive.** Same 6-item batch with two obviously
bad lines mixed in (a joke; a wildly wrong-mood "flowers/springtime"
line). Mistral caught the joke (score 1) but scored the wrong-mood line
**4/5**, nearly as high as genuine matches -- and silently dropped one
item from its output. Switched judge model to
`mlx-community/DeepSeek-Coder-V2-Lite-Instruct-4bit` (code tier, despite
the name -- proved the stronger general instruction-follower all night,
not just for code). Same calibration test: real spread (3,4,1,4,1,4),
correctly caught BOTH violations, zero dropped items.

**Step 4/5, first split -- human spot-check found it too weak to trust.**
Ranked the 6/24 gate survivors + hand-repaired versions of the rejects
(24 total, DeepSeek judge, K=8): approved mean 4.25, rejected mean 3.00.
Luke's spot-check: not confident this was a real quality gap -- several
"rejected" items used strong character-specific motifs (Korrath's
"BOUND" -- central to his whole premise; Edric's "RECOUNT," echoing his
real line) that arguably beat some "approved" items. Decision: don't
spend training compute confirming a contrast a human already can't
confidently agree with -- widen the pool for a real gradient first.

**Widening the pool -- three more failed LLM-generation attempts, then
direct authorship.** All three characters in one dispatch: exactly the
"personality washed out" voice-collapse `guard_corpus.py`'s own header
comment already warns about (isolated dispatches only) -- Bram and
Edric produced near-duplicate lines, and the whole batch genre-broke
into sci-fi/corporate register ("MISSION ACCOMPLISHED," "ALL SYSTEMS
GREEN," "SECTOR EIGHT" -- in a medieval dungeon). Isolated per-character
retry with an explicit "no modern/sci-fi phrasing" instruction: still
genre-broke (plausible negation-backfire -- naming the failure mode may
have made it more salient). Isolated retry with that instruction removed,
reverting to the plain style that worked for attempt 1: still genre-broke
("REINFORCEMENTS NEEDED," "ALL SYSTEMS GREEN," "SOMETHING SUS"). Tried
switching to DeepSeek for generation too: marginally better but still
leaked gamey phrasing ("ALERT LEVEL RISING," "COMBAT MODE" -- breaking
the fourth wall) and dropped half the requested output. **Both available
local models reliably fail at sustained in-genre voice control for this
task via simple prompting**, even with strong worked examples -- a real
finding for mechanism 1's eventual design, not just tonight's blocker.
Fell back to direct authorship: 30 new lines written directly (natural
quality variation, not deliberately bimodal), merged with the original
24 into a 54-candidate pool, all passing the structural gate and
confirmed non-duplicate of existing corpus content.

**Step 3, second pass -- real discrimination this time.** Same
isolated-per-slot-batch discipline (lesson from the first pass), DeepSeek
judge: full 1-5 range, all 54 items scored, zero dropped. Distribution
{1:1, 2:2, 3:24, 4:21, 5:6}.

**Step 4, second split.** K=6 (matches the natural size of the top
tier): approved mean **5.00**, rejected mean **2.33** -- a real,
defensible gap this time, not 4.25-vs-3.00. Lead's own read agrees with
the judge's ranking: approved leans toward richer, thematically-grounded
lines (Korrath's "bound" motif, "forgotten my own name"); rejected leans
toward flat generic acknowledgments ("fine by me," "not bad, keep it
up"). Written to `trainer/m13_final_split.json`, ready for Luke's actual
spot-check (step 5, still outstanding -- a lead's own read is not a
substitute for it, same discipline as the first split).

**Why step 6 (the four training retrains) was not run tonight, on
purpose.** Two real reasons, not caution for its own sake: (1) the
protocol's own step 5 requires Luke's human spot-check before any
training compute is spent, and that hadn't happened yet when this was
written; (2) this same overnight session hit two real near-OOM dips
(free memory 5%, then 2%) running much lighter workloads than four
consecutive full-cast MPS retrains, with an upgraded but still
imperfect watchdog as the only safety net and nobody awake to react to
a real emergency. Queued, not abandoned -- one message from Luke after
his spot-check is enough to run it.

**Update, later the same overnight session: both baseline runs (step 6,
noise-floor half) completed and were reviewed.** With the kill-capable
watchdog proven and the M12.5-incident-class risk closed by tonight's
own checkpointing work (`train_corpus_conditioned_attr`/
`qat_finetune_attr` now checkpoint on every improving epoch), the two
baseline arms -- which test nothing about the disputed judge-score
premise, since they use zero new fragments -- were judged safe to run
unattended. The approved/rejected arms remain paused for Luke's
spot-check, exactly as above.

| | float val loss | qat val loss | guard+korrath inv/line |
|---|---|---|---|
| baseline, seed A | 0.1341 | 0.1413 | **1.50** |
| baseline, seed B | 0.1339 | 0.1818 | **1.81** |

`noise_floor = |1.50 - 1.81| = 0.31 inv/line`. Float losses nearly
match (0.1341 vs 0.1339) -- the float phase itself looks stable across
seeds. The QAT phase is where the two runs diverge (0.1413 vs 0.1818),
and that's what drives the guard+korrath gap.

**Honest read, not a happy one: 0.31 is a large noise floor relative to
what this mechanism is trying to detect.** M12.2 vs M12.4's own real,
already-confirmed improvement was only ~0.21 inv/line (1.65 -> 1.44).
If ordinary seed-to-seed variance alone can move this metric by more
than that entire prior improvement, the approved-vs-rejected comparison
(step 6's other two arms) will need a genuinely large true effect to
produce a signal clearly above this noise -- exactly the self-calibrating
bar already pre-registered (`(rejected - approved) > noise_floor`) is
built to catch, not sidestep. This doesn't invalidate running those two
arms -- the bar still gives an honest PASS/INCONCLUSIVE/FAIL either
way -- but it does mean Luke should read an INCONCLUSIVE result as
plausible and not surprising, not as evidence the pipeline is broken.

One thing worth separately investigating, NOT done tonight: QAT-phase
variance (0.1413 vs 0.1818, a much bigger spread than the float phase's
own near-identical numbers) is an unexplained asymmetry. Whether that's
inherent to this quantization-aware fine-tuning setup at this scale, or
a fixable instability (learning rate, patience, seed sensitivity in the
fake-quant hooks specifically), is an open question this session didn't
have scope to chase -- flagged here rather than silently absorbed into
"noise."

**Update, second overnight session (2026-07-28/29): the human-spot-check
gate (step 5) was explicitly waived by Luke, not secretly skipped.** The
protocol's own step 5 requires a human spot-check of
`trainer/m13_final_split.json` before spending training compute on the
approved/rejected arms -- that hadn't happened when the first overnight
session ended. This session, Luke was asleep and explicitly authorized
proceeding without it ("do the freaking trainings too... you can do it"),
overriding that gate for tonight specifically. Recorded here plainly
because it's a real methodological fact that affects how much to trust
what follows: the approved/rejected split's face validity was never
independently confirmed by a human before the compute was spent, only by
the lead's own read (noted above) and the judge's own scores.

**Approved-arm result: worse than baseline, not better.**

| | float val loss | qat val loss | guard+korrath inv/line |
|---|---|---|---|
| baseline, seed A | 0.1341 | 0.1413 | 1.50 |
| baseline, seed B | 0.1339 | 0.1818 | 1.81 |
| **approved** | 0.1272 | 0.1291 | **2.44** |

2.44 is worse than BOTH baseline runs, not an improvement -- and already
fails the pre-registered bar's first condition
(`approved <= baseline + noise_floor` = 1.50 + 0.31 = 1.81) on its own,
before the rejected arm even runs. Whatever the rejected arm shows, this
cannot be a clean PASS. The rejected-arm run is in progress now to
complete the comparison and apply the full bar honestly (final verdict
recorded in this file's Definition of Done once both numbers are in).

### Final verdict: FAIL

Rejected-arm run finished. Full table (exact values, not rounded):

| | float val loss | qat val loss | guard+korrath inv/line |
|---|---|---|---|
| baseline, seed A | 0.1341 | 0.1413 | 1.5000 |
| baseline, seed B | 0.1339 | 0.1818 | 1.8125 |
| **approved** | 0.1272 | 0.1291 | **2.4375** |
| **rejected** | 0.1290 | 0.2095 | **2.7500** |

```
noise_floor = |1.5000 - 1.8125| = 0.3125
baseline    = 1.5000 (seed A -- seed B's stated purpose was measuring
              noise_floor, not serving as a second baseline estimate)

condition 1: approved <= baseline + noise_floor  ->  2.4375 <= 1.8125  ->  FALSE
condition 2: (rejected - approved) > noise_floor ->  0.3125 >  0.3125  ->  FALSE (exactly equal)
```

**FAIL, decided by condition 1, not a close call.** Approved misses its
threshold by 0.625 -- exceeding the noise_floor itself, not a marginal
miss the INCONCLUSIVE carve-out was written for. Condition 2 happens to
land exactly ON the noise_floor boundary (0.3125 == 0.3125, to 4
decimal places), which in isolation would read as INCONCLUSIVE -- but that
nuance doesn't rescue the result: PASS requires BOTH conditions, and
condition 1 fails cleanly.

**What actually happened, in plain terms:** BOTH the approved and
rejected fragment sets made guard+korrath coherence worse than doing
nothing (baseline). The judge's RELATIVE ranking was directionally
consistent -- approved (2.44) beat rejected (2.75), same order the
judge predicted -- but that relative signal sits right at the noise
floor, and it doesn't matter anyway: the real question mechanism 4
exists to answer is "does judge-approved content help," and the answer
tonight is no, it actively hurt. Adding K=6 new opener fragments to
guard/korrath's existing banks, however the judge scored them, regressed
coherence rather than improving it.

**Plausible causes, not disentangled tonight (would need follow-up
experiments, not more staring at these four numbers):**
- **K=6 may simply be too few new fragments to move a shared model's
  coherence in a measurable, stable direction** -- diluting/reshuffling
  an existing bank with a small addition could just add noise to what
  the model has to learn, independent of the added lines' quality.
- **The direct-authorship pool (30 hand-written lines, not LLM-generated)
  that fed both splits was never itself tested against a no-new-content
  control** -- it's possible ANY new content at this K, from ANY source,
  would regress this metric, which would mean this result says nothing
  about the LLM-judge premise specifically.
- **QAT-phase instability (already flagged above) is doing real work
  here too**: rejected's QAT loss (0.2095) is notably worse than
  approved's (0.1291) and both baselines' QAT losses (0.1413/0.1818) --
  consistent with, but not proof of, the same unexplained QAT variance
  noted earlier compounding with real content-quality differences.

**Conclusion for mechanism 4, and mechanism 1's real pipeline:** FAIL is
a valid, honest stopping point, same discipline as every other negative
result this project has recorded (M12.2, M12.3, m9.1). The LLM-as-judge
fragment-authoring pipeline does not clear the bar to trust at this K,
this rubric, this corpus -- not because the judge scores were
incoherent (the relative ranking direction was right), but because the
underlying premise (new judge-approved content improves real training
outcomes) did not hold up when actually tested. This does not mean the
idea is dead forever -- the "plausible causes" above are real, distinct,
untried follow-up experiments -- but it does mean nobody should build
mechanism 1's real production pipeline on top of this judge without
first running one of those follow-ups and getting a different answer.

## Pipeline (v1 scope)

1. **Candidate generation.** LLM proposes new opener/body-
   skeleton/catchphrase fragments for guard/korrath's existing bank
   categories (mechanism 1) — not whole free-running lines.
2. **Structural gate (free, before any further LLM spend).** Reject
   fragments with wrong vocab/repetition signature (mechanism 3).
3. **Judge pass on survivors.** Batched, extends `llm_judge.py`'s
   existing dispatch/JSON-parse/`opencoder`-routing machinery (not a new
   client) with a tone/voice-plausibility rubric that includes the D:/M:
   attribute values being targeted — "fidelity to tone" is meaningless
   without knowing which voice archetype is in play.
4. **Density gate.** Fold accepted fragments into the generator, require
   ≥K-combo reuse before they're kept (mechanism 2).
5. **Validation experiment (mechanism 4), once, before trusting this for
   real.** Pre-registered judge-score-vs-real-outcome check.
6. **Train/QAT/verify.** Ordinary next-character cross-entropy + the
   existing QAT recipe, same fixed seeds/snapshot discipline as every
   other milestone. Re-run the raw (unguarded) coherence probe against
   M12.4/M12.5's numbers, specifically on guard/korrath, plus the full
   self-test/XCHK suite. **The probe number is the only signal that
   counts** — not the judge score, per the cautionary precedent above.

## Explicitly cut from v1 scope

- **DPO / preference-pair fine-tuning.** No ranking/DPO training
  infrastructure exists in this codebase today. Park as an M13.1 idea
  only if the bank-distillation path alone doesn't close enough of the
  guard/korrath gap.
- **Line-level judge+rewrite of whole free-running generations** (the
  original pitch's core mechanism) — superseded by mechanism 1 above for
  the reason M9 already demonstrated.

## Practical notes specific to this repo

- **Build on, don't rebuild:** extend `trainer/llm_judge.py`'s existing
  dispatch/parse/routing rather than writing a new batched LLM client.
- **Local-model routing:** `~/bin/opencoder` (`OPENCODER_TYPE=text`,
  Mistral-7B), same as `llm_judge.py` already uses — not a new Qwen3.5
  dependency. Run the memory-audit ritual before any large batch.
- **Claude API calls are a new kind of dependency for this project** if
  used for escalation — real API key, real billing, network dependency
  in the trainer pipeline. A deliberate go/no-go decision, not something
  to wire in quietly.
- Pure trainer/corpus-side work — no `core/`, blob format, or N64 kernel
  changes.

## Definition of done

- [x] Mechanism 4 (pre-registered judge-vs-real-outcome validation) run
      and its result recorded **before** any of the rest is trusted for
      a real corpus change — **FAIL**, recorded above: both the
      approved and rejected fragment sets regressed guard+korrath
      coherence versus baseline, a valid stopping point for this
      milestone, same discipline as M12.3/m9.1
- [x] Structural gate (mechanism 3) implemented and tested
      (`trainer/ngpt_trainer/corpus_gates.py::structural_gate`,
      hardened with 7 more edge-case tests this session)
- [ ] Bank-fragment generation + judge pass, extending `llm_judge.py` --
      **not done, correctly so.** The exploratory judge-scoring work
      that happened (three generation attempts, a judge-model swap
      after a calibration failure, two ranked splits) was real but
      ad-hoc -- never committed as a reusable extension of
      `llm_judge.py`'s dispatch/parse/routing machinery. Mechanism 4's
      FAIL means building that out for real production use is
      correctly out of scope now, per this file's own "valid stopping
      point" rule above -- not an oversight.
- [x] Density-reuse gate (mechanism 2) implemented and tested
      (`trainer/ngpt_trainer/corpus_gates.py::density_gate`)
- [ ] Retrain under current QAT settings; raw coherence probe measured
      against M12.4 (1.44) / M12.5's result, specifically on
      guard/korrath -- **not done, correctly so.** This item means
      retraining the REAL shipped corpus with new bank content folded
      in for production use; mechanism 4's FAIL means that retrain
      would be building on an unvalidated premise, so it's correctly
      not attempted. The four *isolated validation* retrains
      (baseline seed A/B, approved, rejected) that mechanism 4 itself
      required are done and recorded above -- a different, narrower
      thing than this DoD item.
- [x] Full self-test/XCHK suite still green — no regression on the
      structural (trie-guarded) guarantee. Verified via a clean rebuild
      + Ares boot of the shipped M12.1 ROM this session (SELFTEST PASS,
      RSP ON, XCHK PASS, 44 ch/s) — M13 never touched `core/`, the blob
      format, or the N64 kernel, so this is a regression check on
      everything else this session changed, not new M13-specific
      verification.
- [x] Docs: this file finalized, outcome recorded as an honest negative,
      same discipline as every M12.x doc and M9.1. No tag/README status
      row — a host-only negative result, no model/ROM ships, matching
      M12.2/M12.3/m9.1's own precedent exactly.

## What this milestone deliberately does not do

- Does not re-solve gibberish/coherence in general — M12.1's trie guard
  already makes that structurally impossible in the shipped ROM
- Does not judge/rewrite whole free-running lines for direct corpus
  insertion — mechanism 1 explains why, grounded in M9's own measured
  failure
- Does not include DPO/preference-pair training
- Does not touch `core/`, the blob format, or the N64 kernel
- Does not replace the conditioning-mechanism work (M12.2-M12.5) — it's
  a corpus-quality lever that stacks with whichever mechanism wins
- Does not trust the LLM-judge score as a gate on its own — the
  coherence probe (real trained-model output) is the only thing that
  decides success, per the 4.75/4.18-vs-3.36/2.64 precedent
