# M10 — Procedural cast: bosses, the villain, and dungeon-generated NPCs (IN PROGRESS — most content/logic landed, two subsystems still open)

**Status: IN PROGRESS (2026-07-18).** Depends on M8 (archetype/instance
system, proven on `guard`) and **M9** (compositional conditioning — this
milestone's tiers ride on M9's feature-tuple mechanism, not M8's opaque
per-id tags; renumbered from the original M9 slot on 2026-07-17 when
compositional conditioning became M9 itself — see `docs/milestones/
m9.md`). This is where the dungeon crawler's other two cast categories
get built: the recurring named villain and the handful of talking
bosses (persistent, authored-ish, appear across the whole game), and the
NPCs a procedurally-generated dungeon level spawns on the fly (ephemeral,
seeded, no hand authoring per instance).

**What's actually landed so far (honestly scoped — not the full
milestone):**

**1. Three-tier schema** — `NPCDatabase::Tier{FULL,MID,THIN}` and
`isPersistent()` exist as real, tested C++ (`game/src/user/
NPCDatabase.h`/`.cpp`), not just a manifest convention nothing read.
Selena is `FULL`; `spawnInstance()` output is `THIN`. Enforced by
`trainer/tests/test_manifest_schema.py`.

**2. Generalized the archetype system onto M9's compositional scheme** —
until this session, guard (M8, old `N:<id>` scheme) and the M9 cast
(Bram/Fergus/Kragan, 3 hardcoded `DialogueDemo.cpp` profiles) were two
separate, unmerged systems. `NPCDatabase::Archetype`/`spawnInstance()`
now carry `occupation`/`age`/gender, bridged to `NpcService::Profile` via
`profileFor()` — any new archetype needs a personality range + corpus,
not bespoke wiring.

**3. Four new town archetypes** — `pub_patron`, `blacksmith`, `wizard`,
`villager`, pulled forward from M11's originally-planned cast
(`docs/milestones/m11.md`) since it rides the same cheap, already-proven
mechanism and could share this session's retrain. Kid-appropriate
content throughout (`pub_patron`'s high-impulsivity end reads as giddy/
goofy, not intoxicated).

**4. Shadewrath** — a recurring necromancer villain, `tier: full` in
`manifests/dungeon_crawler.json`, full bible (public/private/secret/
fear/desire), dedicated corpus (`trainer/ngpt_trainer/
shadewrath_corpus.py`, his own voice — reusing Selena's shared context
bodies read badly against his formal register, so he got his own).

**5. Combined production retrain** (`trainer/make_m10_blob.py`) —
folded both additions into one retrain rather than two. **Val loss
0.0980** (M9: 0.0985, M9.2: 0.1036 — an improvement despite substantially
more content at the same H=320 capacity), agreement 0.9830 (≥0.95 gate),
3/3 generalization combos non-degenerate. **Honest finding, not
glossed over:** Shadewrath's sampled goldens are the least coherent of
any character — 5 of 6 contain garbled words, against Selena's
near-uniform cleanliness — consistent with M8/M9's own capacity-density
findings, attributable to his thin 960-pair training set (vs. Selena's
36,000). A future pass should raise `SHADEWRATH_PER_COMBO` and retrain,
not treated as fixed by this one.

**6. Hardware-verified** — ROM rebuilt, booted in Ares: **SELFTEST
PASS, RSP ON, XCHK PASS** for all 21 golden prompts (12 curated + 6
compositional-cast + 3 Shadewrath), bit-exact on real hardware. **Not
done yet:** `DialogueDemo.cpp`'s START-cycle NPC-selection array hasn't
been updated to include Shadewrath or the new archetypes — separate
display wiring, not part of training. They're in the model but not yet
selectable in the shipped demo.

Host tests: 8/8 suites green. Trainer tests: 149 green.

**Second wave (2026-07-18), same session — closing out most of the
remaining DoD:**

**7. Korrath** — the mid-tier talking boss, `tier: mid`, guards the
captured elf princess, bound into unwilling servitude by Shadewrath.
Own bible, own dedicated corpus (`trainer/ngpt_trainer/korrath_corpus.py`,
deliberately ~half Shadewrath's density), manifest entry. `mid` tier now
has a real, concrete instance for the first time.

**8. Seeded dungeon-level NPC placement + save/load determinism** —
`DungeonGenerator::npcsForLevel()` deterministically derives which
thin-tier archetypes populate a level and each instance's spawn seed
from a single level seed (xorshift32, same discipline as
`spawnInstance()`). This closes the save/load determinism contract for
free: a "reload" is just re-deriving from the stored seed.
`test_reload_reproduces_identical_dialogue_text()` proves the actual
claim (identical dialogue TEXT, not just identical seeds) end to end.
30 checks, TDD (confirmed red via a failing CMake configure before
`DungeonGenerator.cpp` existed).

**9. Demo wiring** — Shadewrath, Korrath, and the 4 new archetypes are
now real `DialogueDemo.cpp` NPC-cycling slots (14 total, up from 8) —
trained into the model AND selectable in the shipped demo, not just the
former. Compiles clean via the real Pyrite64 build (this file has no
host-test coverage, libdragon dependency).

**10. Density-fix retrain** — raised `SHADEWRATH_PER_COMBO` 8→24
(tripling his pairs) and folded in Korrath's corpus, one combined
retrain. **Honest result: the density fix did not clearly resolve
Shadewrath's coherence gap.** Val loss landed at 0.0995 — slightly
*worse* than the prior M10 retrain's 0.0980, though still better than
M9.2's 0.1036. His golden lines remain a mixed bag (some clean, several
still garbled), and a new failure mode surfaced: content from different
trust tiers bleeding into one response (tier-2 "alliance offer"
language appearing at tier-1). Korrath, brand new this session, shows a
similar mixed pattern. Agreement 0.9804 (still ≥0.95 — this is a model-
fluency limit, not an engine bug). Recorded plainly rather than framed
as a fix that worked; `docs/plan.md`'s Known follow-ups has the full
record.

**11. Long-horizon consistency eval** — `trainer/
eval_shadewrath_long_horizon.py`, a scripted 8-step session across all
3 trust tiers with narrative "known intervening events," reviewed by
human eyeball per the Data Science Review's own instruction (not an
automated pass/fail gate). **Result: mixed, not a clean pass.** Tone
escalation partially breaks down (tier-2 offer language leaks into
tier-1, the same cross-tier bleed from item 10); the actual climactic
tier-2 line came out more garbled than the tier-1 line that
foreshadowed it, undermining the intended payoff; coherence doesn't
reliably hold across the whole session. The eval *exists and was run*
— that was the DoD ask — but Shadewrath does not yet demonstrate real
long-horizon consistency.

**12. Hardware-verified again** — ROM rebuilt, booted: **SELFTEST
PASS, RSP ON, XCHK PASS** for all 27 goldens (15 curated + 6
compositional-cast + 3 Shadewrath + 3 Korrath), including real
`N:korrath` prompts proven byte-exact on hardware. Live interactive
NPC-cycling via START was **not** manually confirmed this session —
Ares uses its own internal input scancode table, not raw macOS
keycodes, and a blind guess at the mapping changed context instead of
NPC, so this wasn't pursued further; the golden-set hardware proof is
stronger evidence anyway (byte-exact, not visual).

**What's still genuinely open, and why it's not being stretched into a
tag:** two real gaps remain, and they're a different scale of work than
anything above — new subsystems, not corpus content or host-testable
pure logic:

- **No actual persistence/save mechanism exists anywhere in this
  project.** `isPersistent()` is a correct rule (`docs/milestones/
  m10.md` section 3) with nothing plugged into it — no save file, no
  EEPROM/cart-save code, no cross-session memory for Shadewrath or
  Korrath. "Persistent across encounters" in the DoD's own wording for
  the bad guy and the boss is not met.
- **`DungeonGenerator::npcsForLevel()` is proven correct in host tests
  but never invoked from the live game loop.** There is no ROM demo
  scene that actually shows a generated level with placed NPCs — the
  DoD's "demo shows one procedurally-generated level with a thin-tier
  guard instance + the bad guy in two separate simulated encounters"
  is not met live in the ROM (the closest this session gets is
  Shadewrath appearing at 3 trust tiers in the golden set, and the
  offline long-horizon eval script).

**Not tagged `m10` — the DoD is substantially further along than the
first checkpoint but still not met.** Building an actual save system
and wiring dungeon generation into a real playable scene are both
real, separate efforts, not a continuation of tonight's content-and-
logic pass.

## Why

Town NPCs are static and hand-placed — M8's archetype system covers
them completely. Dungeon levels are generated "based on rules" per the
game design, and if an archetype instance can appear in a generated
level, its identity, personality jitter, and any placement logic need to
come from the *same* seed the level generator used, or the two systems
drift out of sync in ways that are hard to debug and impossible to
reproduce from a bug report. Meanwhile the villain and bosses are a
categorically different kind of character — they persist across many
separate encounters over a long play session, which is a form of
"continuity of existence" M7 and M8 never actually tested (Selena is
continuous within one town scene; archetype guards don't need memory at
all).

## Engineering plan — with the engineer hat on

### 1. A three-tier cast system, made explicit

The project has, without quite naming it, been building toward a
spectrum rather than a binary (full model vs. shared model) — M10 is
where that spectrum gets a name and concrete rules, because the
dungeon crawler's roster actually needs all three tiers at once:

| Tier | Who | Corpus | Memory |
|---|---|---|---|
| **Full** | Selena, the bad guy | full dedicated corpus (M7-scale, ~10MB) | persistent, long-horizon |
| **Mid** | talking bosses, a few named town figures (pub owner) | archetype-plus, more than a bare instance, less than full | persistent within their own arc |
| **Thin** | generic guards/wizards/damsels/shop patrons | archetype instance (M8), now composed from M9's feature vocabulary | none beyond the current encounter, or none at all |

**Tier is meta-schema, not project content** (M8 §1a) — `full`/`mid`/
`thin` ship as fixed values in the manifest's shape, the same way
`characters[]`/`archetypes[]` do, because the tier boundaries come from
capacity economics that hold regardless of genre, not from this game's
specific cast. The bad guy and any talking bosses are `characters[]`
entries in the project manifest (tier `full`/`mid`), exactly like
Selena — not a new data shape, just more entries.

The bad guy is **not** an archetype instance — he needs the same
treatment Selena got in M7 (dedicated bible, dedicated corpus,
persistent NPC Database entry), because he has to remember the player
across separate encounters and levels in a way no archetype-tier
instance needs to. Talking bosses sit in between: named, with a real if
smaller corpus, persistent only within their own local arc (this boss,
this dungeon branch) rather than the whole game.

**Full/mid tier characters get compositional features too, on top of
their dedicated corpus** (M9) — the bad guy isn't exempt from the
feature-tuple mechanism just because he also has a bible; his profile
features (age/gender/occupation-equivalent/personality descriptor) still
compose with his relationship state per player, the same mechanism, on
top of the dedicated voice content M7-style characters get that
thin-tier instances don't.

### 2. Seeded procedural instantiation — deterministic, not just varied

When the dungeon generator rolls a new level, any archetype-tier NPCs it
places must be instantiated from the *same* seed stream the level
generator uses — same discipline as M4's sampler, M8's instance
jitter, and M9's relationship-state randomization (xorshift32,
deterministic given a seed). Concretely: level seed → (level layout,
thin-tier NPC placements + their instance seeds) as one deterministic
derivation, not two independently-random systems that happen to run in
the same frame. This is what makes "seed N regenerates this exact
level, with this exact guard, saying creatures of this exact
personality jitter" testable at all — and it's the same bit-exactness
discipline the whole project already runs on for the model itself.

### 3. Lifecycle across the dungeon loop

Explicit rule, because it's easy to get wrong silently: **full and mid
tier NPCs persist across dungeon loop iterations** (the bad guy
remembers how many times you've died to him across separate runs; a
recurring boss remembers your last fight). **Thin-tier instances spawned
inside a generated level are ephemeral** — no durable NPC Database entry
survives past that level, they don't carry memory into the next loop,
and their conditioning is dominated by current-level state (mood/event)
rather than history. This keeps the NPC Database's persistent-memory
cost bounded by the *authored* cast (dozens, known in advance), not by
however many levels a long play session generates.

### 4. Mid-save-load determinism contract

If the player saves mid-level and reloads, a thin-tier instance must
either (a) say the exact same thing it would have on first encounter,
reproduced deterministically from the stored level seed, or (b) be
explicitly documented as allowed to drift, with a reason. Pick (a) by
default — it's free (the seed is already stored for level regeneration)
and it's consistent with the project's existing "no undocumented
nondeterminism" discipline. Don't leave this implicit; it's the kind of
gap that turns into a confusing bug report months later.

## Definition of done

- [ ] Three-tier system implemented: full / mid / thin, with the NPC
      Database schema supporting all three (persistent vs. ephemeral
      entries). **Mostly done**: the `Tier` enum + `isPersistent()` exist
      and are tested; `mid` now has a real instance (Korrath). Still
      missing: `isPersistent()` isn't wired to any actual persistence
      mechanism (no save data, no cross-encounter memory anywhere in
      this project) — it's a correct rule with nothing plugged into it
- [x] The bad guy: full bible + corpus, persistent across encounters and
      loop iterations; added to the project manifest as a `characters[]`
      entry (tier `full`), same shape as Selena. Shadewrath has a bible,
      a dedicated corpus (density-raised this session), a manifest
      entry, trained into the shared model, hardware-verified, and
      selectable in the shipped demo. **Caveat kept honest**: "persistent
      across encounters" has no save mechanism behind it (see the tier
      item above), and his sampled coherence — even after a density fix
      — remains inconsistent (landed-work section 10 has the full
      record). Checking this box for what it concretely asks
      (bible/corpus/manifest/shape), not for a claim that he's bug-free
- [x] At least one talking boss: mid-tier bible + corpus, persistent
      within its own arc; added to the manifest as a `characters[]`
      entry (tier `mid`). Korrath — same caveats as Shadewrath above
      (no persistence mechanism, mixed coherence)
- [x] Dungeon-level generation derives thin-tier NPC placement + instance
      seed from the same level seed (one deterministic derivation).
      `DungeonGenerator::npcsForLevel()`, 30 host-test checks
- [x] Save/load determinism contract implemented and tested: reloading a
      level reproduces the same thin-tier instance dialogue.
      `test_reload_reproduces_identical_dialogue_text()` proves the
      actual dialogue-text claim, not just seed equality
- [x] Long-horizon consistency eval for the bad guy across a scripted
      multi-encounter session (Data Science Review). `trainer/
      eval_shadewrath_long_horizon.py`, run and reviewed by human
      eyeball. **The eval exists and ran — that was the ask. The result
      is mixed, not a clean pass** (tone escalation partially breaks
      down, the climactic tier-2 line is more garbled than the tier-1
      line that foreshadows it) — recorded honestly, not treated as
      Shadewrath "passing"
- [ ] ROM boots, SELFTEST PASS, demo shows one procedurally-generated
      level with a thin-tier guard instance + the bad guy in two separate
      simulated encounters. SELFTEST/RSP/XCHK all PASS, but
      `DungeonGenerator` is never invoked from the live game loop — no
      ROM scene actually shows a generated level. Not met
- [ ] Docs: this file finalized with real numbers, README status row,
      tag `m10`

## What M10 deliberately does not do

- The full town cast build-out (shopkeeper/wizard/damsel/pub_patron
  archetypes) — M8 proved the mechanism on `guard`; filling out the rest
  of the roster is M11 content work, not new mechanism
- Toolkit packaging / portability documentation — M11
- Gossip between NPCs (needs the full town, not just one archetype +
  one villain) — M11

## Data Science Review — reviewing the engineering plan above

**The tier corpus budgets need to be derived, not asserted.** The table
above gives "full ~10MB, mid more-than-instance-less-than-full, thin
archetype-instance" as a *shape*, but the mid-tier number is currently a
placeholder with no empirical anchor. Recommend: before authoring a
boss's corpus, run the same divergence-metric methodology from M7/M8 at
a few candidate mid-tier sizes and pick the smallest one that clears a
"boss reads as distinct from both Selena/full-tier voice and from
generic archetype voice" bar — don't guess a number between two known
endpoints and call it done.

**New eval category: long-horizon consistency.** Nothing tested so far
covers a character across *many separate* encounters spread over a long
session with save/loads in between — Selena's M7 eval is continuous
within one scene, archetype instances in M8/M9 don't have memory to be
inconsistent about. The bad guy is the first character where this
actually matters (contradicting an earlier taunt, forgetting an
established score, resetting emotional tone between encounters would all
read as "not alive" even if any single line sounds fine). Concrete
proposal: a scripted synthetic-session eval — N simulated encounters
with known intervening events (player died twice, player found the
villain's stolen item, a session save/load in between) — checked by
human eyeball for continuity, not just per-line quality. This is a new
axis, not a bigger version of the per-condition eyeball check M4/M7
already do.

**Determinism contract for thin-tier instances is a correctness
requirement, not a nice-to-have, and it's cheap to verify.** Since the
level seed is already stored, add it to the golden-vector discipline
this project already runs everywhere else: pick a handful of level
seeds, record the exact thin-tier dialogue they produce, and let CI/host
tests catch any future change that silently breaks reproducibility — the
same "host-green implies console-behavior" logic the whole project is
built on, extended to procedural NPC placement.

**Watch the capacity-monitoring metric started in M8, continued in M9,
now with a sharper question.** M10 adds a full-tier character (the bad
guy) and at least one mid-tier boss to a shared model that's also
carrying Selena and M9's compositional archetypes. This is exactly the
scenario the original one-model-per-NPC-vs-shared-model tradeoff
discussion worried about: capacity dilution across *multiple full-tier
voices*, not just one voice plus thin archetypes. If Selena's or the bad
guy's own held-out divergence/val-loss measurably degrades when the
other is added to the same training run, that's the concrete, data-
backed trigger to split into two shared models (e.g., one for full/mid-
tier "named" characters, one for thin-tier archetypes) rather than a
single universal model — record this as the explicit decision rule, not
something to notice only if it becomes an obvious problem later. M9's
compositional mechanism should make this *less* likely to trigger than
it would have under M8's opaque-tag scheme (shared traits share
capacity instead of competing), but that's a hypothesis M9's own
capacity data should confirm before M10 assumes it.
